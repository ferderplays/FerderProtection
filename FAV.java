import javax.swing.text.html.parser.Entity;

public class A {
    int b = Entity(width = "20px", height = "20px", color = "Green", border = 2px solid red,type="button");if(b.pressed=True)
    {
        do {
            System.scanFiles( in(System.fileDirectories()));
            if(System.foundMalware()) {
                do {
                    this.displayTitle("Malware found!");
                }
            }
        }
    }
}